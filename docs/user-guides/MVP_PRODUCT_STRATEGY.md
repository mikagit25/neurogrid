# NeuroGrid MVP + Full Product Deployment Strategy

## 🎯 Текущая ситуация

### ✅ Что работает на сервере (https://neurogrid.network/):
- **Главная страница** - с работающим AI демо
- **Страница инвесторов** - полная функциональная страница
- **API endpoints** - базовые функции MVP
- **Все ссылки** - корректно ведут между страницами

### 🚧 Что нужно добавить (НЕ ломая MVP):
- **Полнофункциональный продукт** - dashboard, wallet, monitoring
- **Расширенное API** - coordinator server с полным функционалом
- **Hybrid deployment** - MVP + Full Product параллельно

## 🏗️ Стратегия развертывания

### Вариант 1: Hybrid URLs (Рекомендуется)
```
neurogrid.network/           → MVP (текущий рабочий)
neurogrid.network/demo/      → MVP demo interface  
neurogrid.network/investors  → Страница инвесторов (работает)
neurogrid.network/app/       → Full Product (новый)
neurogrid.network/api/       → MVP API (текущий)
neurogrid.network/api/v2/    → Full API (новый)
```

### Вариант 2: Subdomain
```
neurogrid.network            → MVP (текущий)
app.neurogrid.network        → Full Product (новый)
api.neurogrid.network        → Full API (новый)
```

### Вариант 3: Different Port
```
neurogrid.network            → MVP (порт 80)
neurogrid.network:3000       → Full Product (порт 3000)
```

## 📋 План реализации

### Этап 1: Подготовка (без влияния на MVP)
- [x] Создана отдельная ветка `feature/full-product`
- [x] Сохранены изменения в stash
- [ ] Тестирование в локальной среде
- [ ] Создание отдельного Docker Compose для полного продукта

### Этап 2: Parallel Deployment  
- [ ] Настройка nginx для параллельной работы
- [ ] Развертывание полного продукта на `/app/`
- [ ] Тестирование что MVP продолжает работать

### Этап 3: Post Product Hunt
- [ ] После успешной презентации
- [ ] Миграция пользователей с MVP на полный продукт
- [ ] Постепенное выключение MVP

## 🔧 Технические детали

### MVP (сохраняем как есть):
- **mvp-server.js** - текущий рабочий сервер
- **landing-page.html** - главная страница с демо
- **investors.html** - страница инвесторов
- **Порт 3000** - текущий порт

### Full Product (добавляем):
- **coordinator-server** - полное API
- **web-interface** - Next.js приложение  
- **Порт 3001** - coordinator API
- **Порт 3002** - web interface

### Nginx Configuration:
```nginx
# MVP (сохраняем)
location / {
    proxy_pass http://mvp-server:3000;
}

# Full Product (добавляем)
location /app/ {
    proxy_pass http://web-interface:3002;
}

location /api/v2/ {
    proxy_pass http://coordinator:3001;
}
```

## ⚠️ Критические требования

1. **НЕ ТРОГАТЬ main ветку** до Product Hunt
2. **НЕ ИЗМЕНЯТЬ рабочий MVP** на сервере
3. **НЕ ЛОМАТЬ существующие ссылки**
4. **ТЕСТИРОВАТЬ всё в feature ветке**

## 🚀 Следующие шаги

1. **Завершить локальную разработку** в `feature/full-product`
2. **Создать отдельный deployment script** для полного продукта
3. **Протестировать параллельную работу** MVP + Full Product
4. **После Product Hunt** - принять решение о миграции

## 📞 Решение

**Рекомендую**: Продолжать разработку в `feature/full-product` ветке, создать параллельный deployment на `/app/` без влияния на рабочий MVP.

**Timeline**: 
- Сейчас: Доделать полный продукт в feature ветке
- До Product Hunt: Оставить MVP как есть
- После Product Hunt: Развернуть полный продукт параллельно