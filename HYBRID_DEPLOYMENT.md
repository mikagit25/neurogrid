# NeuroGrid Hybrid Deployment Guide

## 🎯 Стратегия развертывания

Этот план позволяет запустить **полный продукт ПАРАЛЛЕЛЬНО с рабочим MVP**, не нарушая существующую функциональность для Product Hunt.

## 🏗️ Архитектура

```
neurogrid.com/              → Landing Page (статическая)
neurogrid.com/demo/         → MVP Demo (СОХРАНЯЕТСЯ как есть!)
neurogrid.com/app/          → Новое полное приложение  
neurogrid.com/api/          → API для полного приложения
neurogrid.com/investors.html → Страница инвесторов
```

## 🚀 Безопасное развертывание

### 1. Локальное тестирование
```bash
# Запуск гибридной конфигурации локально
./deploy-hybrid.sh

# Проверка всех endpoints
curl http://localhost/         # Landing
curl http://localhost/demo/    # MVP (должен работать!)
curl http://localhost/app/     # Full App
curl http://localhost/api/health # API
```

### 2. Проверка функциональности MVP
- ✅ MVP Demo на `/demo/` работает
- ✅ Ссылка на инвесторов работает
- ✅ Все API endpoints MVP сохранены

### 3. Развертывание на сервере
```bash
# На сервере
git pull origin feature/full-product
./deploy-hybrid.sh
```

## 🔒 Безопасность

### Что ГАРАНТИРОВАННО сохраняется:
- ✅ Рабочий MVP на `/demo/`
- ✅ Страница инвесторов
- ✅ Все существующие ссылки
- ✅ Product Hunt готовность

### Что добавляется:
- ➕ Полное приложение на `/app/`
- ➕ Полный API на `/api/`
- ➕ Обновленная landing page с новыми ссылками

## 📊 Мониторинг

### Проверка здоровья системы:
```bash
# Статус всех контейнеров
docker-compose -f docker-compose.hybrid.yml ps

# Логи MVP (для Product Hunt)
docker-compose -f docker-compose.hybrid.yml logs -f mvp-server

# Логи полного приложения
docker-compose -f docker-compose.hybrid.yml logs -f web-interface

# Логи API
docker-compose -f docker-compose.hybrid.yml logs -f coordinator
```

### Важные endpoint'ы для мониторинга:
- `GET /health` - Общее здоровье nginx
- `GET /demo/health` - Здоровье MVP
- `GET /api/health` - Здоровье API
- `GET /app/` - Доступность полного приложения

## 🚨 План отката

Если что-то пойдет не так:

### Быстрый откат к MVP-only:
```bash
# Остановить гибридную конфигурацию
docker-compose -f docker-compose.hybrid.yml down

# Вернуться к простому MVP
docker run -d -p 80:3000 --name mvp-only neurogrid-mvp
```

### Полный откат к рабочей версии:
```bash
git checkout main
# Развернуть предыдущую рабочую версию
```

## 🎯 Roadmap

### Фаза 1: Гибридное развертывание ✅
- ✅ MVP остается нетронутым
- ✅ Добавляется полное приложение
- ✅ Общий nginx proxy

### Фаза 2: После Product Hunt
- 🔄 Перенос пользователей с MVP на полное приложение
- 🔄 Деактивация MVP демо
- 🔄 Полный переход на новую архитектуру

### Фаза 3: Масштабирование
- 🔄 Load balancing
- 🔄 Микросервисная архитектура
- 🔄 Отдельные домены

## 📞 Поддержка

### Если MVP перестает работать:
1. Проверить логи: `docker logs neurogrid-mvp`
2. Проверить nginx: `docker logs neurogrid-nginx`
3. Откатиться к предыдущей версии
4. Связаться с командой

### Контакты экстренной поддержки:
- 🚨 Критические проблемы: немедленный откат
- 📧 Обычные вопросы: GitHub Issues
- 💬 Срочные консультации: команда разработки

## ✅ Pre-flight Checklist

Перед развертыванием на production:

- [ ] Локальное тестирование завершено
- [ ] MVP функциональность подтверждена
- [ ] Все endpoint'ы отвечают
- [ ] Backup текущей версии создан
- [ ] План отката готов
- [ ] Команда уведомлена о развертывании

## 🎉 Success Metrics

### MVP (Product Hunt):
- ✅ `/demo/` отвечает за < 2 сек
- ✅ Демо-форма работает
- ✅ Ссылка на инвесторов работает

### Full Product:
- ✅ `/app/` загружается
- ✅ Dashboard отображается
- ✅ Wallet функционирует
- ✅ API возвращает данные

Этот подход обеспечивает **максимальную безопасность** для презентации Product Hunt, позволяя одновременно демонстрировать полную функциональность инвесторам и пользователям.